# DB 원형 설계정보

ERD 및 각 칼럼 정보

사용자
users       자료형              속성 
id          PK(INT)         /   increment    unique  not_null
name        VARCHAR/String  /   not_null
email       VARCHAR/String  /   unique  not_null
password    VARCHAR/String  /   not_null    hast(not plain)<<< 이었겠지만 지금은 사전값 
created_at  TIMESTAMP       /   nullable
updated_at  TIMESTAMP       /   nullable

게시물       자료형             속성
posts
id          PK(INT)         /   increment   unique  not_null
title       VARCHAR/String  /   not_null
content     TEXT            /   not_null
user_id     FK(users_id)    /   not_null (fk)
created_at  TIMESTAMP       /   nullable
updated_at  TIMESTAMP       /   nullable

E-R  1:N (user - posts)


## 삭제 정책
user.id 를 pk 참조하는 posts
이때 user가 삭제되면? 
1. 전부삭제 (Cascade)
2. 삭제불가 (Restrict)
3. 삭제된 게시자 null처리 (set null)
 > 일단 cascade 선택


## SQL문으로 처리한다면
CREATE TABLE users (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    created_at TIMESTAMP NULL,
    updated_at TIMESTAMP NULL
);

CREATE TABLE posts (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    user_id BIGINT UNSIGNED NOT NULL,
    created_at TIMESTAMP NULL,
    updated_at TIMESTAMP NULL,
    CONSTRAINT fk_posts_user
        FOREIGN KEY (user_id) REFERENCES users(id)
        ON DELETE CASCADE
);

## 요약
users
    id, name, email, password, timestamps

posts
    id, title, content, user_id, timestamps

관계:
    users (1) : posts (N)

외래키 + 삭제정책:
    posts.user_id -> users.id
    ON DELETE CASCADE


이제 이걸 PHP의 migration으로 바꿔야함

기존 migration 파일들 전부 날리고, 내가 만들 최소기능만 구현





