#   이미지 모델 생성

`php artisan make:migration create_post_images_table`


`php artisan storage:link`

pulbic\storage 에 심볼릭 링크 설정
- 실제 로컬위치로는 storage/app/public에 저장 / 브라우저에서 접근할 링크 설정하는것


폼에서 파일 받기 위한 선언부
create.blade에 우선 선언 (나중에 edit.blade도 고치기)

PostController에 /images 로 request 받는거 구현해야함
